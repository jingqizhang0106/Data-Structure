/**
 * This is a node class which is used to construct the singly linked list.
 * Each node contains a data field, and the next node pointer.
 *
 * @author Mia Zhang
 */
public class ListNode {
    Object data;
    ListNode next;

    public ListNode(Object data) {
        this.data = data;
    }
}
